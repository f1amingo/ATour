<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.v7.widget.Toolbar
        android:id="@+id/z_toolbar_address_manage"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorPrimary"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:title="地址管理" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/z_recycler_address_manage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/z_toolbar_address_manage"
        android:layout_marginBottom="48dp"
        android:background="@color/white_parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_alignParentBottom="true"
        android:background="@color/white"
        android:gravity="center">

        <Button
            android:layout_marginBottom="0dp"
            android:id="@+id/z_btn_add_address_manage"
            style="@style/BottomBtnTheme"
            android:text="添加新地址" />
    </LinearLayout>
</RelativeLayout>
